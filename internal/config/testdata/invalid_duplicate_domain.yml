server:
  name: srv2
  deploy_domain: deploy.example.com
  stacks_dir: /opt/deploy

apps:
  budget:
    repo: nogo/budget-app
    branch: main
    domain: shared.example.com

services:
  nextcloud:
    path: stacks/nextcloud
    domain: shared.example.com  # duplicate of budget
